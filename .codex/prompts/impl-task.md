# /impl-task

Pick a task from the implementation plan, auto-detect size, implement, test, commit, and update handoff.

**Argument:** `<task-id e.g. IMPL-003 or task description>`

Follow the workflow defined in `.agents/workflows/impl-task.md`.

Input: $ARGUMENTS
