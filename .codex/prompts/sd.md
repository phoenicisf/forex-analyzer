# /sd

Run System Design → produce 6 design docs in docs/design-docs/02-08 (gaps 01/06 merged into 02) + ADRs + API specs (Phase 1B).

**Argument:** `<optional focus hint, e.g. "focus on payment flow" | "skip Evolution Sequence — greenfield monolith" | leave empty>`

Follow the workflow defined in `.agents/workflows/sd.md`.

Input: $ARGUMENTS
