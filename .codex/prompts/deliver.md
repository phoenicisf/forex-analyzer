# /deliver

Generate final delivery handoff — readiness assessment, documentation finalization, and delivery summary.

**Argument:** `<scope> e.g. 'all' or 'api' or 'web' (default: all)`

Follow the workflow defined in `.agents/workflows/deliver.md`.

Input: $ARGUMENTS
