# /ba

Run BA requirements discovery → produce 5 BA deliverables in docs/ba/01-05 (Phase 1A; v1.2: 06-handoff dropped).

**Argument:** `<optional focus hint, e.g. "MVP only" | "include mobile" | leave empty>`

Follow the workflow defined in `.agents/workflows/ba.md`.

Input: $ARGUMENTS
