Follow CLAUDE.md as the canonical ruleset. MQL5 strict, no external DLLs, modular-monolith layering, and task/state reconciliation through docs/state files. After editing any .mq5 or .mqh, run the 4-gate workflow: G1 compile with MetaEditor, G2 smoke/init evidence, G3 headless tester via terminal64.exe /config:..., G4 log review. Do not mark empirical tasks closed when runtime evidence still fails.