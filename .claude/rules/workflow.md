# PhoenicisNex Workflow Rules

> Stack-agnostic methodology baseline (git/PR/handoff/ADR/Phase Gate) + PhoenicisNex cold-bootstrap recipe.

## Git + PR Workflow
- Branch convention: `feat/<task-id>-<slug>` / `fix/<task-id>-<slug>` / `refactor/<scope>` / `chore/<scope>`
- Commit format: `[type:<service-slug>] short description` + `Why:` line — see CLAUDE.md §5
- Types: feat, fix, refactor, test, docs, chore
- **ห้าม** force push ไป main/develop
- **ห้าม** skip pre-commit hooks (`--no-verify`) unless explicitly authorized
- Every PR MUST include the `simulation/headless-tests/<task>.ini` if task is slot/orchestrator/cross-slot (TD-02 §13.6)
- Compile artifacts (`.ex5`, `.compile.log`) NOT committed (UTF-16LE binary noise)

## Handoff Discipline
- Single project (no monorepo): use `docs/state/current_handoff.md` (created on first task)
- Update handoff: end of feature / session change / cross-day boundary — NOT every micro-step
- Evidence artifacts: `docs/state/_session-handoff/<task-id>-evidence-*.{log,json,jsonl,md}`
- 3-file propagation rule: `impl-plan.md` → `overview.md` → `current_handoff.md` (CLAUDE.md §6 State Reconciliation)

## ADR Discipline
- 12 ADRs locked (001-012) — design QA certified 2026-05-02
- New architectural decision → `docs/adr/NNN-<kebab-title>.md` (NNN = next sequential)
- ADR amendment (e.g., adding "Revisit-when" trigger) → log in ADR + cite in commit
- ADR conflict resolution: `/backtrack sd` (changes the decision) vs `/amend td` (changes implementation skeleton only)

## Phase Gate Discipline
- 5-Phase methodology (DESIGN → DESIGN QA → IMPLEMENT → HARDEN → DELIVER) per CLAUDE.md §7 Glossary
- Three-Tier Closure (Task / Walk / Phase Gate) per CLAUDE.md §1 — never conflate
- For PhoenicisNex specifically: Tier 1.5 walk artifact = headless backtest + Tester log + journal audit (no GUI walk)
- Phase Gate ปิดไม่ได้จนกว่า: all `[x]` task ACs + Tier 1.5 walk artifact ≤14d + `IMPL-Pn-GATE` task `[x]`

## Cold-Bootstrap Recipe (PhoenicisNex)

> Per user remark 2026-05-02: developer MUST be able to bootstrap from a clean state and run G1-G4 reproducibly. Derived from 3 MT5 SKILLs.

### Bootstrap from cold (assumes MT5 installed per `origin.txt`)

```bash
# 1. Resolve install + data dirs
ORIGIN=$(cat origin.txt | tr -d '\r')                     # e.g. "C:\Program Files\FBS MetaTrader 5ph"
TERMINAL_ID=$(basename "$(pwd)")                          # 32-char hex
DATA_DIR="/c/Users/$USER/AppData/Roaming/MetaQuotes/Terminal/$TERMINAL_ID"
TESTER_LOG_DIR="/c/Users/$USER/AppData/Roaming/MetaQuotes/Tester/$TERMINAL_ID/Agent-127.0.0.1-3000/logs"

# 2. Compile entry .mq5 (G1)
METAEDITOR=$(echo "$ORIGIN" | sed 's|^\([A-Z]\):|/\L\1|; s|\\|/|g')/MetaEditor64.exe
"$METAEDITOR" /compile:"$DATA_DIR/MQL5/Experts/PhoenicisNex/PhoenicisNex.mq5" /log

# 3. Verify compile log (G1 pass)
iconv -f UTF-16LE -t UTF-8 "$DATA_DIR/MQL5/Experts/PhoenicisNex/PhoenicisNex.compile.log" \
  | grep -E "Result:|error" | tail -5
# Expect: "Result: 0 errors, 0 warnings, NNNN ms elapsed"

# 4. Confirm foreground MT5 is closed (data-dir lock prevents headless launch)
Get-Process -Name terminal64 -ErrorAction SilentlyContinue | Select-Object Id,MainWindowTitle

# 5. Smoke run (G3) — uses standard bootstrap_smoke.ini per TD-02 §13.6
bash .agents/skills/mt5-headless-backtest/scripts/run_headless_backtest.sh \
     simulation/headless-tests/bootstrap_smoke.ini /tmp/bootstrap_run.txt

# 6. Inspect bootstrap log (G2 + G4 combined — init_ok + no [ERROR])
grep -nE "\[Phoenicis\]|init_ok|\[ERROR\]" /tmp/bootstrap_run.txt | head -30

# 7. Inspect journal (G4) — sample 5 records vs schema
head -5 MQL5/Files/PhoenicisNex/journal/tester/run-*.jsonl | jq .
```

### Teardown (between iterations)

```bash
# Reset state for fresh boot test (DESTRUCTIVE — confirms cold-bootstrap path)
rm -f MQL5/Files/PhoenicisNex/state/state.json
rm -f MQL5/Files/PhoenicisNex/journal/tester/run-*.jsonl
# Then re-run Step 5-7 of bootstrap above; expect log:
# [system][ev=state_corrupt_starting_fresh] + state restored to defaults per NFR-3.1
```

### Smoke spec invocation (single command)

```bash
# Per-task smoke ini lives at simulation/headless-tests/<task>.ini
bash .agents/skills/mt5-headless-backtest/scripts/run_headless_backtest.sh \
     simulation/headless-tests/<task>.ini /tmp/<task>_run.txt
```

## CI Hook (advisory — not auto-written)

> Suggested CI step (e.g., GitHub Actions on Windows runner with MT5 installed): run `bootstrap_smoke.ini` against a clean clone on every PR. PhoenicisNex CI infrastructure is NOT YET established — when added, file would land at `.github/workflows/mt5-smoke.yml` and call the same `run_headless_backtest.sh` script.

## Phase 5 Closure mechanical gates (engineer-side, per-task)

> Added 2026-05-04 per impl-plan rebuttal-round-07 §07.1/07.2/07.3/07.4/07.5 recommendations; expanded 2026-05-05 to 11 gates after R16 caught build-integrity + commit-hygiene defect classes. Run **all 11** before TL;DR `Last updated:` rewrite + commit. Catches R06/R07 defect classes at task-3 (per-closure) instead of task-33 (per-review-cycle).

| # | Gate | Command | Pass criteria |
|---|------|---------|---------------|
| 1 | **Forbidden-pattern grep** | `grep -cnE "deferred per .* precedent\|deferred to operator-runtime\|structurally complete.*deferred\|live verification deferred" docs/state/impl-plan.md` | `0` hits — if non-zero, reword AC text + register E-AC in `deferred-ac-registry.md` instead of inline `[x]` defer |
| 2 | **TL;DR ↔ registry recount** | `awk -F'\|' 'NR>13 && /^\| P[0-9]/ {gsub(/ /,"",$2); print $2}' docs/state/deferred-ac-registry.md \| sort \| uniq -c` then compare with `impl-plan.md` line ~8 `Deferred-AC Active:` row counts | per-phase counts + total match TL;DR claim exactly |
| 3 | **TL;DR ↔ matrix denominator** | Verify TL;DR `Phase N x/y` denominator `y` matches `Phase × Size matrix § Total` for that phase (e.g., P4=17 not P4=11) | denominator equals matrix Total column |
| 4 | **Sentinel counter increment** | After closing task, bump `Plan Staleness Sentinel § Closures since last review` by +1 atomically with TL;DR `Last updated:` rewrite | counter = (post-review closures); section + TL;DR self-flag agree |
| 5 | **overview.md sync** | Update `docs/state/overview.md § Impl Plan` row Last Updated date + status string append (per CLAUDE.md §6 State Reconciliation Discipline 3-file rule) | overview.md Last Updated == today; status string mentions latest closure |
| 6 | **File integrity (post-Edit-batch)** | `grep -c "^## End of Plan" docs/state/impl-plan.md` then `tail -3 docs/state/impl-plan.md` | exactly `1` `## End of Plan` marker; tail shows clean closure with no orphan fragments / partial words / dangling `---` |
| 7 | **Phase Status Snapshot Notes sweep** | Re-read `## Phase Status Snapshot` table row of the relevant phase; verify Notes column reflects post-closure reality (Mid-Phase Audit status, next-task pointer, threshold flags) | Notes column does NOT contradict TL;DR / Audit Log narrative / Sentinel; no stale "🚨 THRESHOLD CROSSED" or stale "Next:" pointing to a closed task |
| 8 | **Narrative-section freshness sweep** | Re-read `## Open Risks` (each R-N row) + `## Next Best Action` checklist; rewrite or strikethrough rows whose claims are invalidated by this closure | no Open Risks row references closed tasks as still-open; Next Best Action top unchecked item == TL;DR `Next:` pivot |
| 9 | **Post-fix grep verification (impl-review-fix only)** | After a fix-round commits land: (a) re-run the originating R-finding's pattern grep with `--count` against the touched dir(s); (b) ALSO run a broadest-class grep that matches the *intent* of the finding (e.g., if the finding is "remove references to closed task X", use `grep -rcE "deferred to <task-id>(\+\| \|\.\|$)"` against the whole `MQL5/Experts/PhoenicisNex` tree, not just literal `<task-id>+` against one subdir); **(c) R16 strengthening — repo-wide intent grep:** ALSO run the same broader-class regex against the *entire repo* `grep -rcE <pattern> .` (or equivalent `docs/ MQL5/ .claude/ scripts/ simulation/`) — state-doc + audit-doc surface MUST track the source-tree narrative; "whole repo tree" means the repo root literally (`.`), NOT a chosen subdir; narrowing to `MQL5/...` alone is the same regression class fix-round-15 § 9b accidentally introduced (caught by R16 § 16.3 + 16.7); record ALL THREE post-conditions in the fix-round report. **(d) R18 catalog expansion — closed-task forward-pointer verb catalog:** when the finding is "remove forward-pointer comments to closed task NNN", the source-tree + test-config sweep MUST cover every verb form (`wires? at IMPL-NNN`, `wire at IMPL-NNN`, `wired at IMPL-NNN`, `populated by .* at IMPL-NNN`) AND every separator form (`/`, ` + `, ` `, `..`, trailing `+` suffix) AND any inline parenthetical form (e.g., `(IMPL-NNN + IMPL-MMM 5-yr regression)`, `(IMPL-NNN / IMPL-MMM RiskManager::OpenOrder)`). R18 §18.1 caught the `wires at IMPL-017 / IMPL-062` + `at IMPL-053+` + parenthetical variants that R16 §16.3 missed (R12 → R13 → R14 → R16 → R18 chain) — the originating regex catalog grew from one literal phrase to seven verb-separator combinations. **(f) R21 destination-existence verification:** when a fix introduces a bin-1 routing comment that cites `<file>:<line>` or `<class>::<method>`, the engineer MUST grep-verify the destination is not a banner-comment-only hit — `grep -nE '<method>\\s*\\(' <file>` or `grep -nE 'public:|protected:|private:.*<method>' <file>` MUST return ≥1 non-banner site (function definition or call). Engineer MUST attest the verification in the fix-round narrative (cite the line:method pair). R21 §21.2 caught `core/Orchestrator.mqh::WireSlots step 4` cited at 2 sites in fix-round-20 §20.1 Pass-1 — `WireSlots` exists only in 2 banner blocks, no callable method. **(g) R21 token-collision pre-check:** when a fix-round chooses a bulk-substitution token (e.g., a comment-routing pointer string), the engineer MUST inspect ≥5 representative call-sites and verify the token's internal nouns do NOT collide with the surrounding verb prose (e.g., a token containing "wiring" produces "wires at wiring path" / "wired at wiring path" doubling at every `wire[ds]?` prefix). Token-collision pre-check post-condition: `grep -rcE "<verb-class> at <token>"` against the source tree → 0 hits with grammatical doubling. R21 §21.1 caught `Orchestrator wiring path (core/Orchestrator.mqh)` token — 34 sites with `(wire[ds]?|wire) at <token>` doubling defect that fix-round-20 §20.1 Pass 2's "31-file grammatical-doubling cleanup" missed (it cleaned suffix doubling, not prefix doubling). **(h) R22 line-anchor brittleness rule:** bin-1 routing comments MUST cite the destination by grep-stable anchor (symbol name, comment marker, or function definition), NOT by physical line number. Line numbers MAY appear as ancillary navigation aids but MUST NOT be the load-bearing anchor — drift on file edits silently desyncs the cite without compile-time signal. R22 §22.1 caught `domain/CSlotBase.mqh:68,150` citing `core/Orchestrator.mqh` "line 365-369" as the load-bearing pointer; reworded to cite the grep marker `"wire CPipMath into every slot post-RegisterAll"` instead. Verification post-condition (R22): `grep -nE "\\bline [0-9]+(-[0-9]+)?\\b" <comment-block>` MUST return 0 hits OR each surviving line-number annotation MUST be paired with a grep-stable symbolic anchor in the same comment block. **R23 strengthening (tree-wide scope + word-boundary regex):** the line-anchor sweep MUST run tree-wide (`MQL5/Experts/PhoenicisNex/`), NOT scope-narrowed to the cited file (same regression class fix-round-15 §9b accidentally introduced; re-narrowed in fix-round-22 §22.1 verification). Verification regex MUST use `\\b` word-boundary anchors to avoid false-positive matches inside compound words (e.g., `inline 5/3-digit` should NOT match). For each surviving hit, the engineer MUST verify the cited symbol actually exists at the cited line ± 2-line band; sites failing verification are realized drift and MUST be re-anchored to a grep-stable symbolic anchor (symbol name, comment marker, or function definition). Doc anchors (`TD-02 §X line N`, `ADR-NNN line N`) are exempt per separate doc-drift discipline. Combined sweep regex: `grep -rnE "\\b(line [0-9]+(-[0-9]+)?|\\.(mqh\|mq5):[0-9]+(-[0-9]+)?)\\b" MQL5/Experts/PhoenicisNex/ \| grep -vE "(TD-02 (§\|line)\|ADR-[0-9]+ (§\|line)\| § [0-9]+(\\.[0-9]+)? line \|(trade-journal-schema\|state-persistence-schema\|slot-abstraction-contract)\\.yaml \|MACD line\|Signal line\|EMA line\|SMA line\|RSI line\|legacy line [0-9]+\|(PhoenicisN2\\.10_stable\|LibCommon[0-9.]+)\\.mq5:[0-9]+)"`. R23 §23.1 caught 3 realized-drift sites (`Slot_GO.mqh:15,99` cite `Slot_G.mqh:392` — TriggerGOverload not at 392; `SlotState.mqh:38` + `PortfolioState.mqh:176` cite OnTradeTransaction `(line 791)` — non-compliant per (h) text even when correct) + 2 borderline non-compliant sites (`BootstrapValidator.mqh:615` cited `:402-415`, `Orchestrator.mqh:199` cited `(PhoenicisNex.mq5:41)`). All 8 sites re-anchored to grep-stable symbolic markers in fix-round-23. **R24 strengthening (extended exemption regex):** the exemption regex previously covered only `TD-02 §` / `ADR-NNN §` / `TD-02 line` / `ADR-NNN line`. Review-round-24 §24.1 ran the documented combined regex literally and surfaced 5 surviving non-exempt-by-regex hits that fix-round-23 §23.1 had hand-classified as exempt: (1) abbreviated TD-02 anchor `(per § 7.4 line 1659)` at `core/Orchestrator.mqh:339`; (2)(3)(4) spec-yaml anchors `trade-journal-schema.yaml line 36` / `state-persistence-schema.yaml § cross_slot_state line 119-121` at `helpers/Timestamp.mqh:7,26` + `services/CrossSlotCoordinator.mqh:693`; (5) TA-indicator false positive `MACD line 1=Signal line` at `services/MarketContextBuilder.mqh:359`. Extended exemption regex covers four classes: **(α) abbreviated TD-02/ADR anchor** `TD-02 (§\|line)` + `ADR-[0-9]+ (§\|line)`; **(β) bare-§ doc anchor** ` § X.Y line ` (handles `(per § 7.4 line 1659)` form); **(γ) spec-yaml anchors** `(trade-journal-schema\|state-persistence-schema\|slot-abstraction-contract)\.yaml`; **(δ) TA-indicator false-positive filter** `MACD\|Signal\|EMA\|SMA\|RSI line`. Engineer running clause (h) MUST verify the documented combined regex returns 0 hits tree-wide WITHOUT further hand-classification (per clause (i) below) — surviving hits force regex extension or scope-out enumeration in the fix-round narrative. **(i) R24 exemption-regex tree-wide verifiability** — exemption regexes used inside Gate #9 verification post-conditions (clause (h) and any future clause that introduces an exemption filter) MUST themselves be tree-wide-verifiable. Engineer MUST run the FULL combined regex (intent grep + exemption filter) tree-wide and verify the documented mechanism reproduces the claimed pass count without further hand-classification. Surviving hits NOT covered by the exemption regex MUST either (a) extend the exemption regex to cover the new class + re-run + cite the extended regex in the fix-round narrative, or (b) be enumerated in the fix-round narrative as scope-out exceptions with stated reason (encoding artifact, doc-drift discipline, etc.). Hand-classified exemptions that the documented regex does NOT reproduce are the exact regression class the R20→R23 chain accumulated (catalog → destination → anchor), now surfacing at the **exemption-mechanism layer** (R24's 4th axis). Verification post-condition (R24): the combined regex (intent grep `\|` exemption filter) executed tree-wide on `MQL5/Experts/PhoenicisNex/` MUST return either (1) 0 hits, OR (2) ≤N hits each individually justified in the fix-round narrative with explicit scope-out reason — narrative-only "hand-classified as exempt" without regex extension is forbidden. **R26 strengthening (class (ε) frozen legacy-file cites):** the exemption regex extended with class **(ε) frozen legacy-file cites** `legacy line [0-9]+\|(PhoenicisN2\\.10_stable\|LibCommon[0-9.]+)\\.mq5:[0-9]+` after review-round-26 §Finding 26.1 re-ran the documented Combined sweep regex tree-wide and surfaced 22 hits where R25 §Termination Test claimed 1. Of those 22, 19 fell into the legacy-file-cite class (cites to the **frozen** legacy MQL5 source files `PhoenicisN2.10_stable.mq5` + `LibCommon1.1.mq5` + free-form `legacy line NNNNN` idiom) — semantically compliant (cited files won't drift since they are read-only commit artifacts) but mechanically non-exempt-by-regex per the previous (α/β/γ/δ) coverage. Authorial attestation: legacy files are explicitly preserved as historical reference for the rewrite-vs-baseline regression chain (BR-1.1 + NFR-1.1 + IMPL-061 baseline parser); the cite drift impossibility is structural (legacy `.mq5` files are not in the active source tree, only consulted by the rewrite's per-slot CodeWiki §X.Y mirroring discipline). Predicted post-R26 combined-regex tree-wide return: 1 hit (mojibake at `BootstrapValidator.mqh:81` continues to scope-out per clause (i)(b) encoding-artifact reason, pending IMPL-FIX-013 actual cleanup); the 2 rewrite-file cites at `Slot_B.mqh:209` + `Slot_K.mqh:170` (R26 §Finding 26.3) are resolved by re-anchoring to grep-stable symbolic markers in fix-round-26 commit `3ba3f3d`, not by exemption-regex expansion. **5th-axis "reviewer-authoring contract" surfaced (R26):** the R12→R24 chain's 4 axes (catalog/destination/anchor/exemption-regex) were declared "terminated" by R25; R26 surfaces a meta-layer above — a verify-only review-round can drift from its own published mechanism (R25 reported 1 hit when the documented mechanism returned 22). Future Gate #9 clause (j) candidate: "review-round termination claim re-verification" requiring the reviewer to re-run the full meta-grep + cite empirical hit count + classify each surviving hit; out-of-scope for fix-round-26 per Finding 26.1 fix recommendation — methodology-evolution belongs in a future `/update-config` ticket per R14 §14.4 precedent. | (a) exit code 1 of the originating grep AND (b) zero matches of the broadest-class grep on the source tree AND (c) zero matches repo-wide OR each surviving hit annotated "preserved as audit history (commit log of round NN closure)" AND (d) clause (h)+(i) combined regex returns 0 hits tree-wide OR each surviving hit individually scope-justified (no hand-classification without regex extension) — any non-zero hit on (b), (c), or (d) means the finding's/rule's intent was wider than the executed sweep; force the engineer to either expand the sweep/regex or explicitly scope-out the non-target sites in the fix-round narrative |
| 10 | **Stash-clean G1 (R16 addition)** | After commit, run `git stash --include-untracked && "$METAEDITOR" /compile:MQL5/Experts/PhoenicisNex/PhoenicisNex.mq5 /log` → inspect `.compile.log` → `git stash pop`; verify post-stash compile = `0 errors, 0 warnings`. Catches HEAD-vs-working-tree drift (R16 § 16.1 build-integrity defect class — caller in HEAD, callee body uncommitted in working tree → fresh clone fails) | exit code 0 + log shows clean compile against the committed surface only |
| 11 | **Working-tree clean post-closure (R16 addition)** | `git status --porcelain \| wc -l` after fix-round / task closure | exit count `0` (all artefacts referenced in fix-round narrative + closure tables — code, evidence sidecars, walk-batch logs, review/fix-round docs — are committed). Untracked review-round / fix-round / evidence files explicitly disallowed (R16 § 16.2 audit-trail gap) |

> **Why this is here:** R06 (2026-05-03) caught 20 forbidden-pattern hits accumulated across 33-task closure burst; R07 (2026-05-04) caught 1 forbidden-pattern regression introduced 24h after R06 closure + 3 state-reconciliation drifts; **R08 (2026-05-04) caught trailer file corruption (3× `## End of Plan` + dangling fragments) + Phase Status Notes column frozen at pre-IMPL-057 state + Open Risks R-6 stale + Next Best Action checklist frozen at P1 era** — all rebuttal-introduced or rebuttal-missed defects in narrative-parallel sections that gates #1-#5 don't cover. Gates #6-#8 push enforcement to **rebuttal-output verification** + **intra-plan parallel-narrative sweep** (TL;DR ≠ only canonical source — Phase Status Snapshot + Open Risks + Next Best Action are also reader-facing snapshots). **R13 (2026-05-04)** added gate #9 after fix-round-12 § 12.8 advertised "all 11 slot files" updated with stale "deferred to IMPL-053+" wording, but actual grep showed 21 sites in 17 slot files survived the sweep (Finding 13.2 HIGH); a single `grep -c "deferred to IMPL-053"` post-condition in the fix-round body would have caught the under-delivered scope at commit time. Same gate would have surfaced R12 § Finding 12.6 / 12.2 SelfTest omissions (R13 Findings 13.5 / 13.6) as scope-narrower-than-narrative. **R14 (2026-05-04)** strengthened gate #9 with clause (b) after fix-round-13 § 13.2 closed "17 slot files / 23 sites" with grep `IMPL-053\+` returning 0 hits — but the broader regex `deferred to IMPL-053` (no literal `+`, repo-wide, not just `slots/`) showed 23 OTHER stale sites still extant (10 in `slots/` without `+`, 13 in `services/`/`core/`/`spike/` with `+`). Same root cause as R12 § 12.8 → R13 § 13.2 (the originating grep is scope-narrower than the defect class). Clause (b) forces the engineer to verify against the **defect class**, not just the **literal pattern from cited sites**, breaking the next-coarser-granularity recurrence chain. **R16 (2026-05-05)** added clause (c) + Gates #10/#11 after fix-round-15 § 9b narrowed the broader-class sweep to `MQL5/...` only — `docs/state/impl-plan.md` accumulated 25 stale `deferred to IMPL-053+` rows that the source-tree-only sweep missed. Same root cause as R12 → R13 → R14 chain (next-coarser-granularity scope narrowing). Clause (c) forces literal repo-root `.` for the intent grep. Gate #10 (stash-clean G1) catches HEAD-vs-working-tree drift (caller committed, callee body uncommitted = fresh-clone build fail — Finding 16.1 CRITICAL). Gate #11 (working-tree clean post-closure) catches commit-hygiene gaps where fix-round narratives advertise "applied + verified" but ~500 LOC of edits + evidence sidecars + review docs sit uncommitted (Finding 16.2 HIGH). **R20 (2026-05-05)** strengthened Gate #9 with clause **(e) — closed-task list MUST be derived dynamically from `docs/state/impl-plan.md` Phase Status Snapshot, NOT hand-enumerated from the cited fix-round-N sites**. fix-round-19 §19.2 hand-enumerated `IMPL-(006\|007\|018\|042\|043\|053)` and missed IMPL-060 + IMPL-062 (closed 2026-05-04/05) → review-round-20 §20.2 caught both. Stage-1: `grep -oE 'IMPL-0[0-9]{2}' docs/state/impl-plan.md \| sort -u > /tmp/closed-impl-tasks.txt`; Stage-2: loop the verb-form catalog against each task. Fix-round narrative MUST attest the dynamic sweep. R20 also added the **comment-history-exemptions manifest** at `docs/state/comment-history-exemptions.md` (per R20 §20.3) — sites in the manifest are subtracted from the dynamic sweep; manifest additions REQUIRE engineer attestation in the fix-round narrative. **R21 (2026-05-05)** added clauses **(f) destination-existence verification** + **(g) token-collision pre-check** after fix-round-20 §20.1 Pass-1 (hand-fix) cited a non-existent `core/Orchestrator.mqh::WireSlots step 4` at 2 sites (only banner-comment hits — Finding 21.2) AND fix-round-20 §20.1 Pass 2 chose bulk token `Orchestrator wiring path (core/Orchestrator.mqh)` which produces grammatical-doubling at every `(wire[ds]?|wire) at <token>` site (34 source sites, 21 files — Finding 21.1). Both defects break the "destination-correctness" axis the R12→R20 chain set out to address. Clause (f) forces grep-verification that cited methods/lines are real (not banner-only); clause (g) forces inspection of ≥5 representative call-sites for natural-prose collision before bulk substitution. R21 §21.3 also tracked the comment-history-exemptions populate work as IMPL-FIX-004 (deferred-ac-registry P5 row, expiry 2026-05-19) — manifest framework landed in fix-round-20 §20.3 but the ~86-site enumeration deferred via tracked ticket, not silent placeholder. **R22 (2026-05-09)** added clause **(h) line-anchor brittleness rule** after fix-round-21 §21.2 hand-fix used physical line range `365-369` as the bin-1 routing anchor at `domain/CSlotBase.mqh:68,150`. Cite was correct at edit time but silently desyncs on any future Orchestrator.mqh edit ahead of line 365 — next-finer-granularity surface of the destination-correctness defect class. Clause (h) forces grep-stable anchors (symbol name / comment marker / function definition) as load-bearing pointers; line numbers demoted to ancillary aids only. R22 verify-only sweep also confirmed R12→R21 chain termination at both axes (catalog via clause (e); destination via clauses (f) + (g)) — broader-class intent grep against the new through-form token returned 0 hits on the source tree with no new defect class manifesting. **R23 (2026-05-09)** strengthened clause (h) with tree-wide scope post-condition + word-boundary regex after fix-round-22 §22.1 applied clause (h) surgically to 2 cited `domain/CSlotBase.mqh` sites only — review-round-23 §23.1 ran independent tree-wide intent grep finding 3 realized-drift line-anchor sites (`Slot_GO.mqh:15,99`, `SlotState.mqh:38`) + 2 borderline non-compliant sites (`BootstrapValidator.mqh:615`, `Orchestrator.mqh:199`); fix-round-23 also caught a 6th site (`PortfolioState.mqh:176`) the reviewer missed via the same OnTradeTransaction `(line 791)` cite class as SlotState.mqh + a 7th + 8th borderline site (`Slot_P.mqh:126` cited `Slot_BI.mqh line 89-95`; `CSlotBase.mqh:156` cited `helpers/PipMath.mqh:31`). All 8 sites re-anchored to grep-stable symbolic markers. **Methodology-scope axis surfaced (NEW R23):** every newly-authored Gate #9 clause MUST be verified tree-wide on its first verify pass, NOT scope-narrowed to the cited sites — same pattern as the catalog axis (R20) + destination axis (R21) but at the meta-level (the rule itself is the new substitution unit). Future Gate #9 clauses MUST include "tree-wide verification post-condition" in the clause body, not as a deferred footnote. **R24 (2026-05-09)** extended clause (h) exemption regex + added clause (i) **exemption-regex tree-wide verifiability** after review-round-24 §24.1 ran the documented combined regex literally and surfaced 5 surviving non-exempt-by-regex hits that fix-round-23 §23.1 had hand-classified as exempt (abbreviated TD-02 anchor `(per § 7.4 line 1659)`; spec-yaml anchors `trade-journal-schema.yaml`/`state-persistence-schema.yaml`; TA-indicator false-positive `MACD line 1=Signal line`). The "0 hits ✅" claim was reproducible only by hand-classification — the documented mechanism (the regex itself, as embedded in the rule body) returned 5 hits. Same root cause as the R20→R23 chain (catalog → destination → anchor): a mechanism authored for the common case + edge cases hand-classified out, then exception coverage drifts as new classes emerge. Clause (i) forces the documented exemption mechanism to be tree-wide-verifiable on its own — surviving hits must either extend the exemption regex (with attestation) or be enumerated as scope-out exceptions in the fix-round narrative. **Exemption-regex axis (NEW R24, 4th axis):** the chain's complete axis set is now {catalog (R20), destination (R21), anchor (R22-R23), exemption-regex (R24)}. Each axis surfaces one meta-level above the prior — defect → fix scope → rule application → rule mechanism. R25 verify-only sweep should re-run the full meta-grep over (a)-(i) and declare chain termination if all 9 clauses verify clean simultaneously WITH the documented mechanisms (no hand-classification).

## Failure escalation
- G1-G4 failure → log in task notes + ≤30-line snippet → fix locally OR escalate via `/backtrack sd` / `/amend td`
- Phase 5 mechanical-gate failure → fix the artifact (reword AC / recount registry / fix denominator / bump Sentinel / sync overview.md / clean trailer / sweep Phase Status Notes / refresh Open Risks + Next Best Action / re-run post-fix grep / stash-clean G1 / commit untracked artefacts) — do NOT mark task complete with `[x]` until all 11 gates pass
- ห้าม silent skip — TD-02 §13.5 audit contract
