description = "Pick a task from the implementation plan, auto-detect size, implement, test, commit, and update handoff"
prompt = """
Follow the workflow defined in `.agents/workflows/impl-task.md`.
Input: {{args}}
"""
