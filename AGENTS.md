# AGENTS.md — AI Agent Entry Point

> Multi-IDE Agent Primer. All AI coding agents (Claude Code, Codex CLI, Windsurf, TRAE, Antigravity, etc.) should read [CLAUDE.md](./CLAUDE.md) as the authoritative source of project rules.

## Quick Context
- **Project:** PhoenicisNex
- **Type:** MetaTrader 5 Expert Advisor (EA) — single-instrument retail Forex EA (EURUSD H4)
- **Methodology:** ANDM Full Track v1.2 (see `constitution/`)
- **Tech Stack (summary):**
  - Language: MQL5 (MetaQuotes Language 5) — strict, no external DLLs
  - Runtime: MetaTrader 5 client (Windows; intra-process EA)
  - Architecture: Modular Monolith (5 layers: core/slots/services/domain/helpers) per ADR-001 + ADR-012
  - Storage: File-based — state.json (atomic) + journal/*.jsonl (JSON-Lines) + MT5 GlobalVariable
  - Testing: MT5 Strategy Tester (headless) + MetaEditor compile + log review per 3 SKILLs

## Where to Look
- **Rules (canonical):** [CLAUDE.md](./CLAUDE.md)
- **Per-IDE rule adapters:** `.claude/rules/`, `.windsurf/rules/`, `.trae/rules/`, `.codex/rules/`
- **Codex CLI config:** `.codex/config.toml` (model + sandbox + agent concurrency) + `.codex/{agents,prompts}/` (methodology-managed)
- **Claude Code commands:** `.claude/commands/`
- **Design docs:** `docs/ba/`, `docs/design-docs/`, `docs/technical-design/`, `docs/adr/`, `docs/api-specs/`
- **MT5 SKILLs:** `.agents/skills/{mt5-headless-backtest,mql-developer,mt5-log-reader}/SKILL.md`

## Critical Workflow Note (per user remark 2026-05-02)
After writing/editing any `.mq5` or `.mqh`, developer MUST run **4-gate Definition of Done**: G1 compile (MetaEditor) → G2 smoke (init_ok in Experts log) → G3 headless backtest (`terminal64.exe /config:*.ini`) → G4 log review (iconv + grep + jq). See CLAUDE.md §6 + `.claude/rules/workflow.md`.

Last generated by `/project-init` on 2026-05-02. Do not edit manually — regenerate via `/project-init --regen`.
