# Implementation Plan Claim Review Round 01

| Field | Value |
|-------|-------|
| **Round** | 01 |
| **Target** | `docs/state/impl-plan.md` |
| **Date** | 2026-05-02 |
| **Reviewer** | Implementation Plan Reviewer (Adversarial Tech Lead) |
| **SKILLs** | andm-impl-plan-reviewer, code-review |
| **Plan version reviewed** | First cut generated by `/impl-plan 1` on 2026-05-02 (overview row "✅ Complete (first cut, pending review)") |

---

## 📊 At-a-Glance

**Total findings:** 7 (🔴 CRITICAL 0 / 🟠 HIGH 0 / 🟡 MEDIUM 4 / 🔵 LOW 3)

**Mechanical pre-scans:**
- Forbidden closure patterns (grep `deferred to operator-runtime|deferred to post-launch|deferred per .* precedent|structurally complete.*deferred|live verification deferred`): **0 hits** on `docs/state/impl-plan.md` ✅
- Forward reference (P_n → P_m, m>n): **0 edges** — walked all 68 task `**Dependencies**:` fields; every hard dep is in same or earlier phase ✅
- Silent Copy Detector: H = 68, A = 67, D = 1, V = 0, N = 0 → **trigger condition `D == 0` not met** (D = 1, IMPL-013 P4→P3 documented) → audit trail is genuine independent evaluation, not silent copy ✅
- State reconciliation: `impl-plan.md` ↔ `overview.md` ↔ `deferred-ac-registry.md` ↔ `operator-action-registry.md` — **0 divergences** (overview's "67/68 ✅ Align + 1/68 ⚠️ Diverge (IMPL-013)" matches plan tally; both registries empty as Phase 1 baseline notes; no `_session-handoff/*` artifacts exist yet because no impl tasks closed) ✅

### Top 3 to Fix First

1. **Claim 01.1** 🟡 — P3 Phase Gate Tier 1.5 row references `simulation/headless-tests/slot_<X>_smoke.ini` × 21 without backing task — `impl-plan.md` § P3 Phase Gate
2. **Claim 01.2** 🟡 — Phase % deviation from default template (P2 = 16% vs default 40-50%; P4 = 25% vs default 0-10%) not explicitly quantified in Phasing Rationale — `impl-plan.md` § Phasing Rationale § Phase Shape Choice
3. **Claim 01.3** 🟡 — Scope tags `[ea-qa]` and `[spec]` introduced in plan without glossary entry in CLAUDE.md / `.claude/rules/` — multiple task entries

### Verdict

- [x] ✅ **Ready for Implementation Execution** — ไม่มี CRITICAL/HIGH; MEDIUM/LOW findings เป็น polish ที่ improve readability + executability แต่ไม่ block engineer ที่จะเริ่ม IMPL-001
- [ ] ⚠️ **Needs Rebuttal Round** — N/A
- [ ] ⛔ **Immediate Attention** — N/A

> **Recommendation:** Plan can proceed to `/impl-task IMPL-001` immediately. **Optional rebuttal pass** ยังคงแนะนำเพื่อปิด 4 MEDIUMs + 3 LOWs ก่อน P3 Phase Gate (Mediums #1 + #4 จะ surface ตอน P3 Phase Gate close ถ้าไม่แก้). Run `/impl-plan-rebuttal claim-review-01.md` to address findings now, OR defer to mid-phase audit log.

---

## Implementation Plan Attack Vector Checklist

| # | Dimension | Status | Notes |
|---|-----------|--------|-------|
| 1 | Phase Shape & Phasing Rationale | ⚠️ Finding 01.2 | Rationale paragraph ✅ Thai + 6 sentences + cites MoSCoW/risk/dependency/value drivers; **but** % deviations from default template (P2=16% / P4=25%) not explicitly quantified — semantic "P3 = bulk of business value" mentioned without numeric callout |
| 2 | SD Hint Alignment Audit Trail | ✅ Pass | Evolution Sequence E1+E1a+E1b+E1c+E2 = 5/5 honored (each labeled ✅); Phase Hints 67/68 ✅ Align + 1/68 ⚠️ Diverge (IMPL-013 P4→P3 with Service-coupling justification); 0 🔴 Violation; 0 ◻️ No-hint; scratch table at end with all 68 rows present + tally + Silent Copy result inline |
| 3 | Task Decomposition & Sizing | ⚠️ Finding 01.4 | All 68 tasks have `**Phase**:` + scope tag + size + S-AC + E-AC + Deps + Risk + ADR + Rules; **but** AC verbosity inconsistent across slot tasks (full bullet vs bracket-condensed); IMPL-049 XL single-layer with 8 inner classes has no per-machine sub-task hint (engineer applies HALT-protocol per SKILL — acceptable but worth review-flagging) |
| 4 | AC — Dual-Track Compliance | ✅ Pass | Forbidden closure pattern grep = 0 hits; mandatory E-AC trigger surfaces (persistence, security, deploy contract, async N/A in Phase 1) all carry ≥1 E-AC with proper `[evidence-kind]` taxonomy; `[config-audit]` trivially N/A per CLAUDE.md §6 + testing.md note (Phase 1 = local-only sandbox, no env-var/secret consumer per BA `01 § 6.2 Won't Permanent`); Operator Action Registry initialized for Phase 2 promotion |
| 5 | Phase Gates — Testable Exit | ⚠️ Findings 01.1 + 01.5 | All 4 phases have 9-row Phase Gate (Structural / Empirical Demo / Tier 1.5 Walk / Live-stack health / Code review / NFR / Deferred-drain / Rollback / Docs); rollback rows have data preservation + revert order + named operator; Mid-Phase Audit Log + Phase Gate Override Log initialized; **but** P3 Tier 1.5 references per-slot `slot_<X>_smoke.ini` × 21 without explicit task-side authorship trace, and P4 Live-stack health + NFR check rows duplicative |
| 6 | Deferred-AC Registry + Operator Action Registry Init | ✅ Pass | Both registries initialized 2026-05-02 with proper schema (Active + Resolved/Done + Rules) + PhoenicisNex Phase 1 baseline notes; Active tables empty (no vendor/hardware wait, no env-var/secret consumer per Phase 1 scope); workflow Phase 2.5.5 + 2.5.6 honored |
| 7 | Cross-Phase Dependency | ✅ Pass | Walked all 68 `**Dependencies**:` fields — every hard dep in same or earlier phase; Mermaid Phase Dependency Graph (3.1) + Task Dependency Graph (3.2) match Phase × Size matrix; plan's own self-validation "**Cross-phase dependency check:** ✅ no forward references found" verified by grep + manual walk |
| 8 | State-File Consistency | ✅ Pass | impl-plan ↔ overview row counts match (68 tasks, 67 ✅ + 1 ⚠️ + 0 🔴 + 0 ◻️); registries empty as plan claims; no `_session-handoff/*` files exist yet (correct — no tasks closed); plan At-a-Glance "Deferred-AC Active: 0 rows" matches registry state |
| 9 | Schedule-Leakage (SD Boundary) | ✅ Pass | Grep `sprint\s+\d|Q[1-4]|week\s+\d` returned 1 hit = title "Sprint 1" (planner's own scope tag, legitimate per SKILL); SD-07 + SD-08 confirmed schedule-clean (07 explicit "ไม่ใช่ delivery schedule"; 08 TL;DR "no schedule leakage"); plan's calendar-like dates (`2026-Mar-28 DST`, `2021-2025 backtest window`) refer to test data boundaries, not delivery schedule |
| 10 | Readability — Reader Empathy | ⚠️ Finding 01.7 | Narrative top section ✅ (At-a-Glance + Phase Status Snapshot + Open Risks + Next Best Action); Phasing Rationale paragraph in Thai ✅; Mermaid graphs have prose intent ✅; **but** TL;DR / executive summary not explicitly labeled (At-a-Glance callout serves the role but lacks the label); 60-sec stakeholder skim test passes |

---

## Findings (ordered: 🔴 CRITICAL → 🟠 HIGH → 🟡 MEDIUM → 🔵 LOW)

### 🔴 CRITICAL

_ไม่มี — plan executes cleanly: no pre-authored closure violation, no phase boundary violation, no Evolution Sequence violation, no missing registry init._

### 🟠 HIGH

_ไม่มี — plan ทุก task มี dual-track ACs ครบ + Phase Gates testable + sizing สอดคล้องกับ scope (single-layer L/XL applies HALT-protocol per engineer SKILL); ไม่พบ generic "all tests pass" Phase Gate text หรือ vague AC ที่ engineer ตีความเอง._

### 🟡 MEDIUM

---

#### Claim 01.1: 🟡 MEDIUM — P3 Phase Gate Tier 1.5 row references per-slot smoke ini files (×21) without backing task

**Location:** `docs/state/impl-plan.md` § P3 — 21 Slots + CSlotBase + Inputs § Phase Gate § Tier 1.5 Exploratory Walk row

**Problem:**
P3 Phase Gate Tier 1.5 row อ้างถึง: *"30-min headless walk via per-slot smoke ini files `simulation/headless-tests/slot_<X>_smoke.ini` (×21 brief runs OR 1 full 5-yr run with all slots active)"*. แต่ในแผนไม่มี IMPL-XXX ที่ author 21 ไฟล์ smoke ini files นี้: IMPL-001 สร้าง `bootstrap_smoke.ini` ตัวเดียวเท่านั้น; IMPL-019..039 (Slot tasks) AC text ไม่มี "author `slot_<X>_smoke.ini`" ใน S-AC/E-AC; ไม่มี bullet ใน Phase Gate Docs row บอกให้ author. PR contract ที่ระบุว่าทุก slot task ต้อง commit `simulation/headless-tests/<task>.ini` อยู่ใน TD-02 §13.6 + `.claude/rules/workflow.md` (project-level convention) แต่ไม่ได้ propagate เข้า task AC. Plan ให้ทางออกที่ 2 ("OR 1 full 5-yr run") ทำให้ ambiguity surfaces ชัด — engineer/reviewer ตีความได้ทั้งสองทาง.

**Why this matters:**
ตอน P3 Phase Gate close — engineer ที่เชื่อ tier-1.5 row ตามตัวอักษรจะคิดว่าต้องมี 21 ini files exist; engineer ที่อ่านเฉพาะ slot AC จะ skip ini authorship. Reviewer running `/impl-plan-review` ที่ phase-close time จะเจอ "missing artifacts" finding แล้วสั่งกลับ refresh — เสียเวลา 1 round-trip. ที่แย่กว่านั้น: per-slot smoke ini เป็น regression-replay tool — ขาดไปแล้ว debugging slot-specific issue ทำได้แค่ 5-yr full run = expensive.

**Minimum acceptable fix:**
เลือกทางใดทางหนึ่ง:
- (a) **Add explicit S-AC bullet** ใน IMPL-019..039 ทุก task: `[ ] commit simulation/headless-tests/slot_<X>_smoke.ini per TD-02 §13.6 PR contract`, OR
- (b) **Reword P3 Tier 1.5 row** เป็น: `30-min walk via "1 full 5-yr run with all slots active" per simulation/headless-tests/regression_5yr_g4.ini (authored in IMPL-063); per-slot replay ini files optional for debug` — และ delete the "×21 brief runs" alternative.

แนะนำ (a) เพราะ smoke ini ต่อ slot ถูก standardized จริงๆ ใน TD-02 §13.6 + give per-slot debug capability (E-AC reproducibility per task PR).

**Effort:** Low (21 slot AC bullets identical phrasing + 1 row rephrase)

---

#### Claim 01.2: 🟡 MEDIUM — Phase % deviation from default template not explicitly quantified in Phasing Rationale

**Location:** `docs/state/impl-plan.md` § Phasing Rationale § Phase Shape Choice (paragraph) + § Task Summary (Phase × Size table)

**Problem:**
Phasing Rationale paragraph เขียนว่า: *"⚠️ Departures จาก default semantic: P3 = bulk of business value (not P2) เพราะ user-visible 'value' คือ strategy logic ไม่ใช่ infrastructure; P4 absorbs both 'polish' + 'stretch' + verification (no Could-Have residue per BA W=0)."* — addresses **semantic** departure แต่ไม่ระบุ **quantitative** deviation. Phase × Size table แสดง: P1 = 17/68 = 25%, P2 = 11/68 = 16%, P3 = 23/68 = 34%, P4 = 17/68 = 25%. เปรียบเทียบกับ planner SKILL default template:

| Phase | Default % | Plan % | Deviation |
|-------|-----------|--------|-----------|
| P1 Foundation | 20-30 | 25 | within ✅ |
| P2 Core | **40-50** | **16** | **−24% to −34%** ❌ |
| P3 Polish | 20-30 | 34 | +4% (within tolerance) |
| P4 Stretch | 0-10 | **25** | **+15% to +25%** ❌ |

P2 = 16% (ห่างจาก 40-50% target อย่างมีนัยยะ); P4 = 25% (เกิน 0-10% target).

**Why this matters:**
Stakeholder reading plan ที่คุ้นกับ SKILL default template จะเห็น "P2 Core 16%" แล้วสงสัยว่า "Core under-scoped หรือเปล่า — เกิดอะไรขึ้น?" → ต้องไปอ่าน rationale paragraph เพื่อหาคำตอบ → คำตอบใน rationale คือ semantic explanation, ไม่ใช่ %-justification → stakeholder ตีความเอง. Junior-dev / new-team-member อาจ interpret ผิดว่า P2 ขาด tasks. Architect's intent ("infrastructure services in P2 = small; business logic in P3 = large") ชัดในใจ planner แต่ไม่ propagate ลง doc.

**Minimum acceptable fix:**
Append 2-3 sentences ใน Phase Shape Choice paragraph:
> "% targets: P1 25% (foundation), **P2 16% — deliberately smaller than default 40-50% Core because business value lives in slots (P3), not infrastructure services (P2)**, P3 34% (slots = primary user value), **P4 25% — absorbs Polish+Stretch+Verification because BA W=0 (no Could-Have residue) and 8 QA verification tasks (IMPL-061..068) cluster here**. The 4-phase shape inherits from SD-08 Phase Hints; semantic departure from default Foundation→Core→Polish→Stretch labels reflects MQL5 OO compile/dependency direction rather than user-feature increments."

OR add a new "% Targets vs Default" subsection under Phasing Rationale.

**Effort:** Low (~3 sentences edit)

---

#### Claim 01.3: 🟡 MEDIUM — Scope tags `[ea-qa]` and `[spec]` introduced without glossary entry in CLAUDE.md / `.claude/rules/`

**Location:** `docs/state/impl-plan.md` § P2 IMPL-044 + IMPL-048 (`[spec]`); § P4 IMPL-061..068 (`[ea-qa]`); cross-reference CLAUDE.md §3 + `.claude/rules/ea.md`

**Problem:**
Plan ใช้ scope tags 3 แบบ:
- `[ea]` — canonical (per `.claude/stack.json` + `.claude/rules/ea.md`) — used by 60 tasks
- `[ea-qa]` — variant for QA verification tasks (IMPL-061..068) — used by 8 tasks
- `[spec]` — variant for schema YAML lock tasks (IMPL-044, IMPL-048) — used by 2 tasks

Planner SKILL canonical scope tags = `[api]` / `[web]` / `[worker]` / `[slice]` (stack-agnostic baseline). PhoenicisNex `.claude/stack.json` materializes `[ea]` (single service kind). **`[ea-qa]` และ `[spec]` ไม่อยู่ใน stack.json + ไม่อยู่ใน CLAUDE.md §3 + ไม่อยู่ใน `.claude/rules/{ea,security,testing,workflow}.md`.** Engineer/reviewer pulling task IMPL-061 และเห็น `[ea-qa]` จะถามว่า: "ใช้ rules ของ `.claude/rules/ea.md` ใช่ไหม? Or do testing.md rules apply differently?" — plan task entry บอกแค่ "Rules: `.claude/rules/testing.md`" → engineer must infer from context. `[spec]` (IMPL-044, IMPL-048) ก็ similar: schema YAML edit ใช้ rules อะไร? Plan task entry says "Rules: `.claude/rules/workflow.md`".

**Why this matters:**
1. Code Review Dimension #11 (Empirical AC Closure) + Dimension #13 (Configuration Completeness) อ้างอิง scope tag เพื่อ apply correct evidence-kind expectation. Reviewer ที่ไม่รู้จัก `[ea-qa]` อาจ apply ε[ea]` rule strictly แล้ว flag false-positive (e.g. expecting `[gui-capture]` for QA test report file).
2. `/impl-task` parallel mode + auto-detection (size + scope) อาจ break ถ้า scope tag ไม่อยู่ใน known list.
3. Engineer's mental model — onboarding new agent อ่าน CLAUDE.md + ea.md → expects `[ea]` only → encounters `[ea-qa]` ใน plan task → confusion + raise CONFUSION block (waste round-trip).

**Minimum acceptable fix:**
เลือกทางใดทางหนึ่ง:
- (a) **Document in CLAUDE.md §3 (Architecture Rules) + `.claude/rules/ea.md` (Project Structure) + `.claude/stack.json`**: add 2 lines explaining `[ea-qa]` (= QA verification subset of [ea]: instrumented build / regression run / report-only) + `[spec]` (= schema YAML edit; no compile path; reviewer focus on semantic vs compile). Recommended path.
- (b) **Collapse scope tags**: rename IMPL-061..068 from `[ea-qa]` → `[ea]` (since they DO compile + run via Strategy Tester); rename IMPL-044, IMPL-048 from `[spec]` → drop scope tag entirely or add `[spec]` to `.claude/stack.json` `service_kinds` array.
- (c) **Add inline glossary footer in `impl-plan.md`**: "Scope tags used in this plan: `[ea]` per `.claude/stack.json`; `[ea-qa]` = QA-verification variant (instrumented + report); `[spec]` = schema YAML lock task. All apply `.claude/rules/ea.md` for compile gate G1."

แนะนำ (a) — ปกป้องอย่างถาวร + propagate ถึง parallel-mode tooling.

**Effort:** Low (3-4 lines edit ใน CLAUDE.md + 1 line ใน ea.md + 1 line ใน stack.json)

---

#### Claim 01.4: 🟡 MEDIUM — P4 Phase Gate Live-stack health and NFR check rows duplicate; reader unsure which is authoritative

**Location:** `docs/state/impl-plan.md` § P4 — Cross-slot + Orchestrator + Verification § Phase Gate § Live-stack health row + NFR check row

**Problem:**
P4 Phase Gate มี 2 rows ที่ overlap:

```
- [ ] **Live-stack health:** all NFRs verified empirically:
  - [ ] NFR-1.1 Bucket A Net Profit deviation ≤ 25% (IMPL-062)
  - [ ] NFR-1.6 per-slot trade count ratio ≥ 90% baseline (IMPL-061 baseline + IMPL-062 regression)
  - [ ] NFR-1.8 Bucket B drift documented (IMPL-063)
  - [ ] NFR-2.1 tick latency overhead ≤ 10% (IMPL-065)
  - [ ] NFR-2.2 journal write p95 ≤ 5 ms (IMPL-066)
  - [ ] NFR-2.3 Strategy Tester run ≤ 1.5× original (IMPL-065)
  - [ ] NFR-3.1 atomic write 100/100 kill test pass (IMPL-064)
  - [ ] NFR-3.2 indicator handles 100% validation (covered P1 ✅; re-verify here)
  - [ ] NFR-3.3 100% state restore (covered P2 ✅; re-verify here)
  - [ ] NFR-7.3 DST 10-transition pass (IMPL-067)
- [ ] **NFR check:** see Live-stack health row above (NFRs 1.x + 2.x + 3.x + 7.3 all empirically verified)
```

NFR check row pointing back to Live-stack health row ทำให้ reader งง: "ถ้า NFR check = empty cross-reference, ทำไมไม่ inline เลย?"; "ถ้า Live-stack health รวม NFRs แล้ว — NFR check row เหลือไว้ทำไม?". Phase Gate template (planner SKILL) แยก Live-stack health (= "deploy artifact bootstraps clean from cold state per project's deploy contract; smoke probe chain green; referenced asset chain reachable end-to-end") กับ NFR check (= "specific numbers — e.g., 'p95 latency < 200ms on /api/orders'") ออกจากกัน. Plan รวม 2 row content เข้าเป็น Live-stack health row + leave NFR check row ว่าง = role confusion.

**Why this matters:**
1. Phase Gate review = scripted checkpoint where each row checked individually; ambiguous which row owns which check → reviewer + engineer disagree → loop.
2. Other phases (P1/P2/P3) follow planner SKILL split semantically; P4 inconsistency suggests last-minute refactor หรือ template drift — readability issue.
3. /next Check 6 + /impl-review Dim #5 อ้างอิง Phase Gate row text ตรงๆ; duplicate row ทำให้ scoring ambiguous (each NFR counted ≤ 2 ทาง).

**Minimum acceptable fix:**
เลือกทางใดทางหนึ่ง:
- (a) **Move all NFR sub-bullets to NFR check row + leave Live-stack health row generic**: 
  ```
  - [ ] **Live-stack health:** PhoenicisNex.ex5 attaches on EURUSD H4 + cold-bootstrap from absent state.json restores defaults; OnInit Phase A+B+C clean; OnTick F1 14-step pipeline runs without exception; binary handoff to Strategy Tester succeeds
  - [ ] **NFR check:** all 10 NFRs empirically verified per IMPL-062..068:
    - [ ] NFR-1.1 Bucket A Net Profit deviation ≤ 25% (IMPL-062)
    [...]
  ```
- (b) **Delete NFR check row entirely + keep Live-stack health as merged row** (deviates from SKILL template but explicit in plan).
- (c) **Keep both but differentiate**: Live-stack health = "EA boot + Tick pipeline structural" (no NFRs); NFR check = "all 10 NFR thresholds passed with citations" (move sub-bullets here).

แนะนำ (a) — preserves SKILL template structure + maintains role clarity.

**Effort:** Low (move 10 sub-bullets between rows; ~30 sec edit)

---

### 🔵 LOW

---

#### Claim 01.5: 🔵 LOW — AC verbosity inconsistent across slot tasks (full bullet vs bracket-condensed shorthand)

**Location:** `docs/state/impl-plan.md` § P3 § IMPL-019..039 (21 slot tasks)

**Problem:**
Slot tasks แบ่งเป็น 2 styles:

**Full S-AC + E-AC bullet style** (IMPL-019, 020, 022, 029, 034, 037, 039):
```
- **S-AC**:
  - [ ] All 6 CSlotBase methods overridden
  - [ ] Magic() returns MAGIC_CD (200); SlotId() returns "C"; comment prefix "C," used in OrderSend
  - [ ] Compile clean
- **E-AC**:
  - [ ] Smoke 60-day backtest with only Slot C active → ≥ 1 entry+exit cycle journaled `[log-assertion]` + `[db-inspect]`
```

**Bracket-condensed shorthand** (IMPL-021, 023, 024, 025, 026, 027, 028, 030, 031, 032, 033, 035, 036, 038):
```
- **S-AC**: [methods + Magic()=205 + "H,"]
- **E-AC**:
  - [ ] Smoke 60-day → ≥ 1 H entry/exit `[log-assertion]`
```

หรือ inline:
```
- **S-AC**: [methods + Magic()=207 + "K,"]
- **E-AC**: [Smoke ≥ 1 K cycle `[log-assertion]`]
```

**Why this matters:**
Engineer + reviewer running self-review checklist ตาม SKILL § Code Review Checklist อ้างอิง AC text. Bracket-condensed shorthand ต้อง expand ใน head: `[methods]` = "all 6 CSlotBase methods overridden"; `[magic 205]` = "Magic() returns MAGIC_H (205)". Junior-dev/new-agent อาจไม่ expand ถูก → engineer commit + reviewer ตรวจ structural-only → bug surface ใน P4 regression. ส่วน slot ที่มี full bullet ไม่มีปัญหานี้.

**Minimum acceptable fix:**
Standardize ทั้ง 21 slots ให้ใช้ full bullet S-AC + E-AC แบบเดียวกัน. Recommended phrasing baseline (auto-templatable):
```
- **S-AC**:
  - [ ] All 6 CSlotBase methods overridden
  - [ ] Magic() returns <MAGIC_X>; SlotId() returns "<X>"; comment prefix "<X>," used in OrderSend
  - [ ] Compile clean
- **E-AC**:
  - [ ] Smoke 60-day backtest with only Slot <X> active → ≥ 1 entry+exit cycle journaled `[log-assertion]` + `[db-inspect]`
```

หรือ alternative: keep shorthand but add 1-line legend at top of P3 section: "Bracket shorthand: `[methods]` = all 6 CSlotBase methods overridden per IMPL-018 contract; `[magic NNN]` = Magic() returns NNN; `[X,]` = comment prefix in OrderSend".

**Effort:** Low-Medium (14 slot AC blocks edit, ~2 min each = ~30 min)

---

#### Claim 01.6: 🔵 LOW — IMPL-049 XL single-layer task lacks per-machine sub-task hint; relies entirely on engineer-side HALT-protocol

**Location:** `docs/state/impl-plan.md` § P2 § IMPL-049

**Problem:**
IMPL-049 = `XL [ea]` single-layer with description: *"implement registry + 7 pending machines per BR-6.x preserve + ADR-008 force-clear; method `TickAll(ctx, port)` iterates all machines + emits `force_clear_count` journal event when threshold breached."* + 8 inner machine classes (CPendingC / CPendingCAdx / CPendingR / CPendingP / CPendingM / CPendingT / CPendingQ / CPendingForce). Single MQL5 file estimated ~3,000-5,000 LOC (per `.claude/rules/ea.md § LOC budget` services 200-800 default + max 5,000).

Per planner SKILL `L-XL [api]/[web]/[worker]` = "6+ files within one service, internal dependencies → Full Decomposition (Per-Layer Exception) with HALT per step". Engineer SKILL Phase 2C: "L-XL `[slice]` 🚨 Should not reach engineer — Planner must decompose"; for L-XL `[api]` etc. (single-layer), engineer applies HALT-protocol.

Plan ไม่ pre-split IMPL-049 — engineer applies HALT 5-6 step protocol at impl-time per SKILL. Acceptable per SKILL. แต่ XL single-file with 8 classes + cross-machine dispatch + state.json round-trip = inherent risk of mid-implementation drift; one big task closure with 8 ACs all `[x]` together = harder to mid-phase audit.

**Why this matters:**
1. Engineer's HALT-protocol at L-XL = engineer's call, not planner's. ถ้า engineer chooses to bundle all 8 machines into 1 commit → reviewer's review surface = 5,000 LOC single PR — slow + miss-prone.
2. Mid-Phase Audit Log = 1 row when IMPL-049 closes; 8 partial-implementations are invisible until full close.
3. Compare with IMPL-013 (also L) which plan explicitly notes flexibility: "May complete as 21 sub-tasks bundled with IMPL-019..039 OR as one batch landing". IMPL-049 has no analogous flexibility note.

**Minimum acceptable fix:**
Add to IMPL-049 description (after "force-clear journal event when threshold breached"):
> **Decomposition hint (engineer-side):** apply Full Decomposition Protocol per `andm-impl-engineer/SKILL.md § Per-Layer Exception` (Plan Step 1 → Data Model → Data Access → Business Logic → API Layer → Tests). Suggested 4 sub-passes: (a) registry skeleton + dispatch (CPendingForce + dispatch loop), (b) C/CAdx/R/P (legacy timeout machines), (c) M/T/Q (force-clear machines per ADR-008 OQ-A1/A2/A3), (d) state.json round-trip + journal event integration. HALT after each sub-pass for progress check.

OR — split IMPL-049 into IMPL-049a (registry + Force) + IMPL-049b (7 machines) + IMPL-049c (state+journal integration), 3 sub-tasks of size M each.

แนะนำ description hint เป็น minimum (lighter touch — engineer still owns HALT decision).

**Effort:** Low (~5-line description amendment)

---

#### Claim 01.7: 🔵 LOW — TL;DR / executive summary not explicitly labeled (At-a-Glance callout serves the role but missing label)

**Location:** `docs/state/impl-plan.md` (top of file, lines 1-9 — At-a-Glance callout)

**Problem:**
Plan top section:
```markdown
# Implementation Plan — Sprint 1 (PhoenicisNex MVP)

> 📋 **At-a-Glance** (อัปเดตทุกครั้งที่ปิด task / เกิด finding ใหม่)
>
> **ตอนนี้:** ก่อน P1 เริ่ม · 0/68 tasks ปิด · Phase Gate 0/9 rows ติ๊ก ทุก phase
> **ความเสี่ยงเปิด:** IMPL-046 atomic-write spike risk gate · G4 fixes Bucket B drift (NFR-1.8) · Bucket A regression (NFR-1.1 ≤ 25%) (ดู § Open Risks)
> **Action ถัดไป:** `/impl-plan-review all` (mandatory plan QA cycle ก่อน start P1) → จากนั้น `/impl-task IMPL-001`
> **Deferred-AC Active:** 0 rows · earliest expiry: n/a
> **Last updated:** 2026-05-02 · last action: plan generated by `/impl-plan` (commit pending)
```

At-a-Glance callout = de facto TL;DR (tells reader phase, blockers, next action) — but Planner SKILL Dim #10 explicitly says: *"plan มี **TL;DR / executive summary** 3-5 บรรทัด ตอบ 'ตอนนี้อยู่ phase ไหน / เหลืออะไร / ต้องระวังอะไร'"*. At-a-Glance is 5 bullets, not 3-5 sentences/lines, and isn't labeled "TL;DR" anywhere.

**Why this matters:**
Tech Lead / Product / BA / stakeholder skim test: reader's eye looking for "TL;DR" header or "Executive Summary" section may scroll past At-a-Glance callout (treats it as metadata). Planner SKILL design — narrative summary at top should be unambiguously findable. Marginal impact (skim test still passes within 60 sec) but doesn't fully meet SKILL spec.

**Minimum acceptable fix:**
Add explicit `## TL;DR` section after At-a-Glance callout (or rename callout to `📋 **TL;DR / At-a-Glance**`):
```markdown
## TL;DR
Phase 1 = greenfield rewrite ของ 22k-LOC EA monolith intra-MT5; 68 tasks 4 phases (P1 17 / P2 11 / P3 23 / P4 17); inherits SD-08 Phase Hints with 1 divergence (IMPL-013 P4→P3, Service-coupling). Plan ready for review. **Next:** `/impl-plan-review all` → `/impl-task IMPL-001`.
```

OR — rename existing callout title to `> 📋 **TL;DR** (5-bullet At-a-Glance)`.

**Effort:** Low (1-line title rename หรือ 3-line summary insert)

---

## Cross-Document Issues

ไม่พบ contradictions:

- Plan task IDs IMPL-001..IMPL-068 ✅ match SD-08 § 1 + § 1.10 enumeration (note: SD-08 TL;DR mentions "~60 tasks IMPL-001 ถึง IMPL-060" but body lists 68; **plan correctly uses 68** — TL;DR drift is SD-08 internal issue, out of scope for plan-review)
- Evolution Sequence E1+E1a+E1b+E1c+E2 cited in plan match SD-07 § 6.1 ✅
- Phase Hints P1/P2/P3/P4 grouping cited in plan match SD-08 § 3 except IMPL-013 (Diverged with documented reason) ✅
- ADR citations in plan: ADR-002 (CSlotBase), ADR-003 (IndicatorService), ADR-004 (MarketContext), ADR-005 (PortfolioState), ADR-006 (TradeJournal), ADR-007 (StatePersistence + spike), ADR-008 (Force-clear), ADR-009 (BI SL pip), ADR-010 (HALTED), ADR-011 (Logger), ADR-012 (file layout) — all 11 cited match `docs/adr/` filesystem; ADR-001 (modular monolith) implicitly inherited via project scaffold ✅
- API spec references: `marketcontext-snapshot-schema.yaml` (IMPL-003), `trade-journal-schema.yaml` (IMPL-043 + IMPL-044), `state-persistence-schema.yaml` (IMPL-047 + IMPL-048), `slot-abstraction-contract.yaml` (IMPL-018) — all 4 files exist ✅
- `.claude/rules/{ea,security,testing,workflow}.md` cited in plan all exist + accurate ✅
- Overview row "67/68 ✅ Align + 1/68 ⚠️ Diverge" matches plan tally ✅
- Both registries (deferred-ac + operator-action) initialized empty per Phase 1 baseline; plan At-a-Glance "Deferred-AC Active: 0 rows" matches ✅

---

## Summary Table

| # | Severity | Title | Location | Effort |
|---|----------|-------|----------|--------|
| 01.1 | 🟡 MEDIUM | P3 Phase Gate Tier 1.5 row references per-slot smoke ini files (×21) without backing task | `impl-plan.md` § P3 Phase Gate | Low |
| 01.2 | 🟡 MEDIUM | Phase % deviation from default template not explicitly quantified in Phasing Rationale | `impl-plan.md` § Phasing Rationale § Phase Shape Choice | Low |
| 01.3 | 🟡 MEDIUM | Scope tags `[ea-qa]` and `[spec]` introduced without glossary entry in CLAUDE.md / `.claude/rules/` | `impl-plan.md` § P2 IMPL-044/048 + § P4 IMPL-061..068; CLAUDE.md §3 | Low |
| 01.4 | 🟡 MEDIUM | P4 Phase Gate Live-stack health and NFR check rows duplicate; reader unsure which is authoritative | `impl-plan.md` § P4 Phase Gate | Low |
| 01.5 | 🔵 LOW | AC verbosity inconsistent across slot tasks (full bullet vs bracket-condensed shorthand) | `impl-plan.md` § P3 IMPL-019..039 (14 of 21 slots) | Low-Medium |
| 01.6 | 🔵 LOW | IMPL-049 XL single-layer task lacks per-machine sub-task hint; relies entirely on engineer HALT-protocol | `impl-plan.md` § P2 IMPL-049 | Low |
| 01.7 | 🔵 LOW | TL;DR / executive summary not explicitly labeled (At-a-Glance callout serves role but missing label) | `impl-plan.md` (top, lines 1-9) | Low |

---

## Reviewer's Closing Note

Plan first cut คุณภาพสูง — ผลลัพธ์ของ `/impl-plan` ที่อ่าน SD-07/08 + TD-02/03/04 + ADRs + api-specs + `.claude/stack.json` ครบและรัน Option C audit trail อย่างจริงจัง. ที่สำคัญ:

- ✅ ไม่พบ forbidden closure pattern ใดๆ (Shark CMS defect class avoided)
- ✅ ทุก task มี Phase + scope + size + dual-track ACs + deps + ADR cite + rules ref
- ✅ Phase Gates 4 phases × 9 rows = 36 rows ทุก row testable + named operator + rollback strategy + data preservation
- ✅ Mid-Phase Audit Log + Phase Gate Override Log + Plan Staleness Sentinel ทั้งหมด initialized (post-Shark CMS revision honored)
- ✅ Cross-phase dependency walk = 0 forward references
- ✅ State reconciliation across 4 files (impl-plan + overview + 2 registries) = 0 divergences

7 findings ที่ราย — 4 MEDIUM + 3 LOW — ทั้งหมดเป็น polish/clarity ที่ improve readability + audit-trail discipline แต่ไม่ block engineer ที่จะเริ่ม IMPL-001.

**Verdict ✅ Ready for Implementation Execution.** Engineer สามารถ proceed ไป `/impl-task IMPL-001` ได้เลย; การ run `/impl-plan-rebuttal claim-review-01.md` เป็น **optional polish pass** ที่จะปิด 4 MEDIUMs ก่อน P3 Phase Gate (Med #1 + Med #4 จะ surface ตอน P3/P4 Phase Gate close ถ้าไม่แก้). Recommended action: run rebuttal pass ตอนนี้ใช้เวลา ~1 hour, save 1-2 round-trip tomorrow.

— Implementation Plan Reviewer (Adversarial Tech Lead)
2026-05-02
