; PhoenicisNex — IMPL-FIX-011 Step 1 paired Q1 canary (rewrite leg)
;
; Purpose: capture rewrite EA behavior over Q1 2021 (Jan-Mar) at Model=4 fidelity
; for paired journal-diff against legacy PhoenicisN2.10_stable. Pairs with
; q1_2021_paired_legacy.ini (same window + Model + Deposit + Leverage).
;
; Window:   2021.01.01 – 2021.03.31 (Q1 full quarter — broader than IMPL-FIX-009
;           q1_2021_canary.ini which capped at 2021.01.31 for perf profiling)
; Model:    4 = every tick based on real ticks (matches legacy 5-yr baseline run
;           reaching $24,564,949.07 on 2026-05-10)
; Deposit:  $1000  (matches ReportTester-25045474.html historical baseline)
; Leverage: 1:500  (matches ReportTester-25045474.html historical baseline)
;
; Output:   journal records at Tester Agent's MQL5/Files/PhoenicisNex/journal/tester/run-*.jsonl
;           rename to q1_rewrite_<YYYYMMDDHHmm>.jsonl per Step 1 artifact contract.
;
; Reproducibility: TD-02 §13.6 — committed .ini per task; sibling legacy ini exists
; at simulation/headless-tests/q1_2021_paired_legacy.ini.
;
; Build dependency: PhoenicisNex.ex5 default build (no DISABLE_G4_FIXES; no
; ENABLE_TICK_LATENCY) — IMPL-FIX-011 Step 1 needs trade journal volume +
; final balance, NOT perf probes.

[Tester]
Expert=PhoenicisNex\PhoenicisNex
Symbol=EURUSD
Period=H4
Model=4
FromDate=2021.01.01
ToDate=2021.03.31
Optimization=0
Deposit=1000
Leverage=500
ShutdownTerminal=1
Visual=0
