; PhoenicisNex — Q1 2021 Canary (IMPL-FIX-009 Step 1 profile baseline + Step 3 post-fix re-measure)
;
; Purpose: bound wall-clock for per-tick performance investigation. Window matches
; IMPL-FIX-008 closure-session canary observation (~6-30 sim-day per wall-min →
; 1-month Model=0 ≈ 1-5 wall-min target). After IMPL-FIX-009 perf fix lands, same
; .ini drives Step 3 re-measure for direct speedup comparison (TD-02 §13.6 reproducibility).
;
; Standard headless [Tester] block per TD-02 §13.3 + .claude/rules/testing.md G3.
; Build dependency: PhoenicisNex.mq5 must have `#define ENABLE_TICK_LATENCY` toggled ON
; for Step 1 baseline + Step 3 post-fix; final production .ex5 has it OFF.

[Tester]
Expert=PhoenicisNex\PhoenicisNex
Symbol=EURUSD
Period=H4
Model=0
FromDate=2021.01.01
ToDate=2021.01.31
Optimization=0
Deposit=1000
Leverage=500
ShutdownTerminal=1
Visual=0
