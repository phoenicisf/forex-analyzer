; PhoenicisNex — Cross-Slot ForceCutloss Smoke Test (IMPL-055)
; Purpose: G1/G3 headless compile + Strategy Tester smoke run for the
;          CCrossSlotCoordinator spike harness. Verifies
;          Spike_CrossSlotCoordinator.mq5 compiles + OnInit SelfTest passes
;          (now 13 cases — IMPL-053 carry-forward 7 + IMPL-055 added 6).
;          E-AC [log-assertion]: Look for "[xslot] SelfTest 13/13 PASS" +
;                                "[xslot][ev=selftest_ok]" in Experts log;
;                                0 [ERROR] lines.
; Standard headless [Tester] block per TD-02 §13.3 + .claude/rules/testing.md G3.
;
; Deferred E-AC (BR-8.3 full CD-pair force-cutloss): activates at IMPL-059+
;   Orchestrator wiring — stub CD positions (BUY+SELL fixture) with
;   directional Stoch M10 + MACD D1 confirm signals → ForceCutloss closes
;   only direction-matched losers + journal emits per-ticket exit records
;   with `triggering_function="ForceCutloss"` [db-inspect].
;   Tracked in deferred-ac-registry.md row IMPL-055 (expires 2026-05-18).
; HALTED matrix (`04 § 9.1`): ForceCutloss runs in BOTH RUNNING and HALTED
;   states (exit-side helper per ADR-010 §107).

[Tester]
Expert=PhoenicisNex\spike\Spike_CrossSlotCoordinator
Symbol=EURUSD
Period=H4
Model=4
Optimization=0
Deposit=1000
Leverage=500
ShutdownTerminal=1
Visual=0
StartDate=2024.01.01
EndDate=2024.03.01
